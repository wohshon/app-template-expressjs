# Web application template using bootstrap and expressjs

For my day to day demo use if I need to create a web front 

`npm start` to spin it up, listens on port 3000 or environment variable set at `PORT`
